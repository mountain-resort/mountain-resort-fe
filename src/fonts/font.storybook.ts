// Storybook에서만 쓰는 '가짜 폰트' 설정
export const lora = {
  className: 'font-lora',
  variable: '--lora',
};

export const open_sans = {
  className: 'font-sans',
  variable: '--open_sans',
};

// Storybook에서 합쳐 쓸 className
export const FontClassNames = 'font-lora font-sans';
